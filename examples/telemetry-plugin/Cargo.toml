[package]
name = "scs-sdk-telemetry-example"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Safe Rust telemetry plugin example for the SCS SDK crates"

[lib]
name = "scs_sdk_telemetry_example"
path = "src/lib.rs"
crate-type = ["cdylib"]

[dependencies]
scs-sdk-plugin = { path = "../../crates/scs-sdk-plugin" }

[lints]
workspace = true
